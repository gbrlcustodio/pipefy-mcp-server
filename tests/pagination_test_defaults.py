"""Shared GraphQL ``first`` page size for tests that assert product default pagination."""

# Aligns with defaults in table/webhook tools and PipefyClient facade (``first: int = 50``).
DEFAULT_FIRST: int = 50
