[project]
name = "pipefy-cli"
version = "0.1.0"
description = "Workspace placeholder for the future Typer-based CLI."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pipefy_cli"]
